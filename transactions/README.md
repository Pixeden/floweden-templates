# Floweden App

This app was created with [create-floweden-app](https://www.npmjs.com/package/create-floweden-app).

## Getting Started

1. Copy the env template:

```bash
mv .env.example .env.local
```

2. Fill in the missing values in `.env.local`.

3. Start the dev server:

```bash
pnpm dev
```
